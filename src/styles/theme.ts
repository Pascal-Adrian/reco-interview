import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#b5e600",
    },
    background: {
      default: "#2b2b2b",
      paper: "#232323",
    },
  },
});

export default theme;
